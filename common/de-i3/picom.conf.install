#!/usr/bin/env bash

source $DOTS_SOURCE/base/bash/environment

echo -e "$YEL*** common/de-i3/picom.conf"

if !hash picom 2> /dev/null; then
  echo -e "$GRE==>$TUR Installing picom"
  sudo -E pacman -Syu picom --noconfirm
else
  echo -e "$GRE==>$TUR picom pre-installed!" 
fi
